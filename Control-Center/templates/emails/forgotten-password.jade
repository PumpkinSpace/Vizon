extends layouts/base

block content
	p Dear #{user.name.first},
	p You recently requested a link to reset your vizon password.
	p Please following this link to reset your password:
	a(href=host + link)= host + link
